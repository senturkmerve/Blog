
@{
    ViewBag.Title = "Update";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayoutPage.cshtml";
}

@model BlogProject.UI.Areas.Admin.Models.DTO.ArticleDTO
<section class="content-header">
    <ol class="breadcrumb">
        <li><a href="/Admin/Home/AdminHomeIndex"><i class="fa fa-home"></i> Home</a></li>
        <li><a href="/Admin/Article/List">Forms</a></li>
        <li class="active">Add Article</li>
    </ol>
</section>

<section class="content">
    <div class="row">
        <div class="col-md-6">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">Add Article</h3>
                </div>
                <form action="/Admin/Article/Update" method="post" role="form" enctype="multipart/form-data">
                    <div class="box-body">
                        <div class="form-group">
                            <label for="Header">Header</label>
                            <input type="text" name="Header" value="@Model.Header" class="form-control" placeholder="Header">
                        </div>
                        <div class="form-group">
                            <label for="Content">Content</label>
                            <input type="text" name="Content" value="@Model.Content" class="form-control" placeholder="Content">
                        </div>
                        <div class="form-group">
                            <label for="UserName">User Name</label>
                            <input type="text" name="UserName" value="@Model.PublishDate" class="form-control" placeholder="User Name">
                        </div>
                        <div class="form group">
                            <label for="CategoryID">Choose Category</label>
                            <select name="CategoryID" class="form-control">
                                @foreach (var item in Model.Categories)
                                {
                                    <option value="@item.ID">@item.CategoryName</option>
                                }
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="AppUserID">Choose Category</label>
                            <select name="AppUserID" class="form-control">
                                @foreach (var item in Model.AppUsers)
                                {
                                    <option value="@item.FirstName">@item.LastName</option>
                                }
                            </select>
                        </div>



                    </div>
                    <input type="text" style="visibility:hidden" name="ID" value="@Model.ID" />
                    <div class="box-footer">
                        <button type="submit" class="btn btn-primary">Submit</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>




